const CREATE = 1;
const START = 2;
const JOIN = 3;
const REROLL = 4;
const CHOICE = 5;
const CHANGE = 6;

const actions = {
    CREATE,
    START,
    JOIN,
    REROLL,
    CHOICE,
    CHANGE
}

module.exports = actions;
